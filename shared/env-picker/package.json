{
  "name": "@repo/env-picker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "types": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "format": "prettier --write --ignore-path ../../.prettierignore .",
    "format:check": "prettier --check --ignore-path ../../.prettierignore ."
  },
  "devDependencies": {
    "@repo/config": "workspace:*",
    "@types/node": "25.5.0",
    "eslint": "10.1.0",
    "typescript": "6.0.2",
    "vitest": "4.1.2"
  }
}
